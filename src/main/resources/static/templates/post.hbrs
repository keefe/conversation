			  <div class="entry" id ="{{_id}}">
			    <h1>{{title}}</h1>
			    <h2>{{tagString}}</h2>
			    <p>{{url}}</h2>
			    <p>{{user.displayName}}</h2>
			    <p>{{user.userId}}</h2>
			    <img src = "{{user.imageUrl}}"/>
			    <div class="body">
			      {{body}}
			    </div>
			    <input type="button" class="button-reply id-{{_id}}" value="Reply"/>
			    <input type="button" class="button-view id-{{_id}}" value="View"/>
			  </div>